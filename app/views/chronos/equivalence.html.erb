<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="chrono-equivalence">
        <p class="line-name">
          <%= @chrono.line.place.name %>
        </p>
        <div class="equivalence">
          <div class="waiting-time">
            <%= t '.have_waited', default: "J'ai attendu" %> <span><%= @chrono.total_duration_in_string %></span>
          </div>
          <div class="place">
            <%= t '.place', default: "à #{@chrono.line.place.name}" %>
          </div>
          <div class="joke">
            <%= t '.joke', default: "soit #{@chrono.joke}" %>
          </div>
          <div class="share">
            <%= link_to "", "https://www.facebook.com/sharer/sharer.php?u=http://www.wequeue.com", class: "social-share-button-facebook", target: "_blank" %>
            <%= link_to "", "http://twitter.com/home?status=#{@chrono.sentence_to_share_equivalence} via @wequeue", class: "social-share-button-twitter", target: "_blank" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>